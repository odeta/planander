<% provide(:title, 'Edit profile | ') %>
<% provide(:button_text, 'Save changes') %>

<div class="jumbotron login-form">
  <h1>Edit profile</h1>
  <%= render 'form' %>
  <%= button_to "Delete account", user_path(id: @user.id), method: :delete, class: "btn btn-danger form-control", style: "margin-top: 7px" %>
</div>
